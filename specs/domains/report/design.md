# Diseño — Reportes (REPORT)

## Paquetes (ADR-001, realizado)
- `d3.report`

## Componentes
- `ReporteServlet`: endpoint de reporte (servlet).
- `GeneradorReportes`, `ReporteBaseSvc`, `ReporteEjecucionSvc`, `ReportGenerateFromSql`,
  `ReportesUtil`, `JasperReportCache`.

## DTOs
- `ReportDTO`, `ReporteBaseDTO`, `ReporteEjecucionDTO` y filtros.
