# Requisitos — Reportes (REPORT)

## Funcionales
- R-REP-001: Generar reportes desde SQL/plantilla Jasper.
- R-REP-002: Ejecución con parámetros (`ReporteEjecucionDTO`).

## No funcionales
- NF-REP-001: Cache de reportes (`JasperReportCache`).
- NF-REP-002: Expone vía servlet y vía API externa `/api/getReport`.
